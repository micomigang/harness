# 2026-09-25 · 视觉总览快捷采用

本次仅修改角色 / 场景 / 道具视觉总览 UI，不改变候选生成、Kimi 结构化产物或后端选择逻辑。

## 新增

- 每个已有候选的缩略图卡片直接显示“确认采用”。
- 已采用的卡片直接显示“已采用”，不会重复确认。
- 总览标题右侧新增“一键确认所有当前候选（N）”。
  - 仅处理“已有候选且尚未采用”的资产；
  - 使用当前采用优先 / 最新候选规则；
  - 仍调用现有 `/asset-candidates/{candidate_id}/select` 接口；
  - 不新增第二套锁定状态。
- 单项“查看 / 调整”详情和批量抽图功能保持不变。
