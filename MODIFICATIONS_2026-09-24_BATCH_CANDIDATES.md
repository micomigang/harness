# 2026-09-24 · 批量角色/场景/道具抽图工作台

本次更新为视觉候选工作台增加“同一要求批量抽图”能力，适用于角色、场景、道具三个阶段。

## 主要变化

- 每个资产卡片新增“加入批量”复选框。
- 工作台顶部新增橘色批量面板：
  - 统一输入一条批量要求；
  - 一键对勾选项“各抽 1 张”或“各抽 4 张”；
  - 每个资产仍会带着自己的 Bible / 视觉本地化分析分别生成，不会把所有角色抽成一模一样。
- 保留原有单资产局部抽图流程：
  - 单卡 textarea + “抽 1 张 / 抽 4 张”；
  - “基于这张调整”；
  - “设为采用并锁定”。
- 批量事件写入活动流：`asset_candidate.batch_generated`。
- 后端新增 `/api/workspaces/{workspace_id}/asset-candidates/batch`。

## 交互建议

### 批量法国化
1. 先勾选多个角色。
2. 在橘色批量输入框里写：
   “全部进一步法国化，保留各自年龄、阶层、叙事功能与主要识别色；服装更贴合可信法国环境，不要保留中国特定造型元素。”
3. 点击橘色“对勾选项各抽 1 张”。

### 单角色精修
- 批量首抽后，如果 Grandma 还不满意，再回到她自己的卡片里写局部要求，继续单抽。

