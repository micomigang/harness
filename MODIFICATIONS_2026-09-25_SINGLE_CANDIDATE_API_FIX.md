# 2026-09-25 · 单项“抽 1 张 / 抽 4 张”链路修复

本次只修视觉候选弹窗中的单资产生成链路，不修改角色/场景/道具 Bible、采用状态或下游引用规则。

## 根因

- 弹窗 z-index=1200，但 toast z-index=50，生成中/错误提示被遮挡，看起来像按钮没有响应。
- “抽 4 张”由 Harness 串行请求 4 次 Provider。旧实现只要后续某次失败就整体返回 502；此前已经保存成功的候选不会立即刷新到 UI。
- 弹窗内部没有独立的请求状态/错误区域。

## 修复

- toast 提升到弹窗之上。
- 单项编辑器增加内联状态：生成中 / 成功 / 部分成功 / 失败。
- `POST /api/workspaces/{workspace_id}/asset-candidates` 保留部分成功：
  - count=1：成功返回 1 张；失败明确显示错误。
  - count=4：逐张生成；若前 N 张成功、后续失败，成功结果立即返回并显示为 partial，不再全部“消失”。
- 前端即使请求异常也会重新读取工作区，确保后端已保存的成功候选马上可见。
- 静态资源版本号更新，避免浏览器缓存旧 JS/CSS。

## 接口

“抽 1 张”和“抽 4 张”仍使用同一接口：

`POST /api/workspaces/{workspace_id}/asset-candidates`

区别仅为 request body 的 `count: 1` 或 `count: 4`。
