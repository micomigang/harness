覆盖并完全重启 Harness 后：

1. 进入任一已采用角色/场景/道具 -> 查看/调整。
2. 在局部修改要求中输入一条要求。
3. 点“抽 1 张”：
   - 弹窗内应立刻显示“正在生成 1 张候选，请稍候…”；
   - 成功后新增候选；失败时弹窗内直接显示 Provider 错误。
4. 点“抽 4 张”：
   - 弹窗内显示“正在生成 4 张候选”；
   - 完全成功显示 4 张；
   - 若中途某张失败，已经成功的候选仍立即显示，并显示“已生成 N/4 张”的 partial 信息。
5. toast 应显示在弹窗上方，而不是被遮住。
